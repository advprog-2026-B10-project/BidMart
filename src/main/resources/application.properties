spring.application.name=bidmart

# Supabase Database Configuration (using Supabase Transaction Pooler)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&connectTimeout=30&prepareThreshold=0
spring.datasource.username=postgres.dbatowvfjtdklbmvpovk
spring.datasource.password=BidMartjayaabadi
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000

# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ahmadwasissh@gmail.com
spring.mail.password=jenmzxssmeppsogv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Secret
app.jwt.secret=BidMart_Super_Secret_Key_For_AdPro_2026_Semester_4

# Max concurrent sessions per user
app.auth.max-concurrent-sessions=3

# Session policy when limit is exceeded: revoke_oldest (default) or reject_new
app.auth.session-policy=revoke_oldest

# App Specific
app.frontend.url=http://localhost:3000

# Actuator & Prometheus Monitoring Configuration
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=always
management.prometheus.metrics.export.enabled=true
